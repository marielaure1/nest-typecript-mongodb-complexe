import {
	IsNotEmpty,
	IsOptional
} from "class-validator";
<% if (documentation) { %>
import { ApiProperty, ApiPropertyOptional } from "@nestjs/swagger";
<% } %>
class Create<%= pascalCase %>Dto {
	// PROPERTIES
}

export default Create<%= pascalCase %>Dto;
