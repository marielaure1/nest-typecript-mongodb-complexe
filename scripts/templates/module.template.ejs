import { Module<% if (authMiddleware) { %>, MiddlewareConsumer<% } %> } from "@nestjs/common";<% if (authMiddleware) { %>
import { AuthMiddleware } from "@middlewares/auth.middleware";<% } %>
import { <%= pascalCase %>Controller } from '@modules/<%= kebabCase %>s/<%= kebabCase %>.controller';<% if (moduleService) { %>
import { <%= pascalCase %>Service } from '@modules/<%= kebabCase %>s/<%= kebabCase %>.service';<% } %>

@Module({
  imports: [],
  controllers: [<%= pascalCase %>Controller],
  providers: [<% if (moduleService) { %><%= pascalCase %>Service<% } %>],
  exports: [],
})
export class <%= pascalCase %>Module {<% if (authMiddleware) { %>
  configure(consumer: MiddlewareConsumer) {
		consumer.apply(AuthMiddleware).forRoutes(<%= pascalCase %>Controller);
	}
<% } %>}
